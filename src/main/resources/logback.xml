<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener" />

    <!-- Logging disabled by default. Pass verbose flag to enable file logging. -->
    <root level="OFF" />
</configuration>
