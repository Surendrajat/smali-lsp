# Java Util Logging configuration for LSP4J
# Default: no file logging. Use --verbose to enable at runtime.

# No handlers by default (prevents file creation)
handlers=

# Set default log level
.level=WARNING

# Silence LSP4J noise
org.eclipse.lsp4j.level=SEVERE
